# .NET
backend/**/bin/
backend/**/obj/
backend/**/*.user

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/*.local

# Editors / OS
.vs/
.vscode/
.idea/
*.DS_Store

# Secrets
**/appsettings.*.local.json

# Deploy
# The stack environment the deploy workflows generate. It holds the database
# password and JWT signing key, and is written into the runner's workspace —
# never into a commit.
stack.env
